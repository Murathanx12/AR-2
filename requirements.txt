# Raspberry Pi dependencies for Alfred/Sonny V4
# Install: pip install -r requirements.txt

# Core
pyserial>=3.5
numpy>=1.24

# Vision (R3, R4)
opencv-python>=4.8
opencv-contrib-python>=4.8

# Voice (R1) — VOSK offline STT
vosk>=0.3.45
PyAudio>=0.2.14

# GUI (optional, omit with --headless)
pygame>=2.5

# Pi hardware
picamera2
adafruit-circuitpython-ssd1306
adafruit-circuitpython-neopixel
adafruit-circuitpython-servokit

# Optional — gesture recognition (EC1)
# mediapipe>=0.10

# Optional — Claude API conversation (EC3, needs ANTHROPIC_API_KEY)
# anthropic>=0.40.0
